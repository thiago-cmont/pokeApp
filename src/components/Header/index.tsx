import React from 'react';

import * as S from './styles';

const Header: React.FC = () => {
  return (
    <S.HeaderContainer>
      <S.HeaderTitle>PokeApp</S.HeaderTitle>
    </S.HeaderContainer>
  );
};

export default Header;
