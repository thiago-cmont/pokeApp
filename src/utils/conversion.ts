export const decimeterToMeter = (value: number): string =>
  parseFloat(String(value / 10)).toFixed(2);

export const hectogramsToKilograms = (value: number): string =>
  parseFloat(String(value / 10)).toFixed(2);
